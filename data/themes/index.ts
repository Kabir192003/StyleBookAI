import { Theme } from "@/types/theme";
import { themesSeed } from "./seed";

// Same pattern as data/colors/index.ts and data/fonts/index.ts — add more
// collections here as they're curated.
export const allThemes: Theme[] = [...themesSeed];
